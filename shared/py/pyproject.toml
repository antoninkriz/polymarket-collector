[project]
name = "obdata"
version = "0.1.0"
description = "Shared library for orderbook data services"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "redis>=8.1.0",
    "python-dotenv>=1.2.2",
]

[tool.setuptools.packages.find]
where = ["."]
