[package]
name = "polymarket-orderbook-rust"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Polymarket orderbook library: WS connection pool, REST client, ClickHouse sink, and shared event types used by the polymarket-orderbook-rust-pubsub and polymarket-orderbook-rust-from-pubsub binaries"

[lib]
path = "src/lib.rs"

[dependencies]
# Async runtime
tokio = { version = "1.42", features = ["full"] }
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-webpki-roots"] }
futures-util = "0.3"

# HTTP client (used for both ClickHouse inserts and Gamma API)
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "gzip"] }

# Redis (cache + streams)
redis = { version = "0.27", features = ["tokio-comp", "streams", "connection-manager"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rust_decimal = { version = "1.36", features = ["serde-with-str"] }

# CLI / config
clap = { version = "4.5", features = ["derive"] }
dotenvy = "0.15"

# Logging / tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }

# Errors
anyhow = "1"
thiserror = "2"

# Misc
url = "2"
rand = "0.8"
chrono = "0.4"
uuid = { version = "1", features = ["v4", "serde"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
